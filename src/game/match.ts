export class Match {

    public positionMatch: boolean;
    public soundMatch: boolean;

    constructor(positionMatch:boolean, soundMatch:boolean) {
        this.positionMatch = positionMatch;
        this.soundMatch = soundMatch;
    }
}